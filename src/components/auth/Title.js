import styled from "styled-components";

const STitle = styled.h1`
  font-size: 40px;
  font-family: "Pacifico", cursive;
`;

function Title() {
  return <STitle>Jistagram</STitle>;
}

export default Title;
